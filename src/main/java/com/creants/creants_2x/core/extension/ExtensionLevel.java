package com.creants.creants_2x.core.extension;

/**
 * @author LamHM
 *
 */
public enum ExtensionLevel {
	ZONE("ZONE", 0), ROOM("ROOM", 1), GLOBAL("GLOBAL", 2);

	private ExtensionLevel(String name, int id) {
	}
}
